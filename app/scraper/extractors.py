"""Placeholder module.

The current scraper relies on the JSON returned by the site's bootstrap-table
XHR (captured in `app.scraper.steps.fetch_card_report`), so no DOM extraction
is needed yet. Keep this file as the home for future per-field parsing if the
site changes.
"""
